# Extra dependencies for training / evaluation (extends the inference set).
# Dùng kho PyTorch CUDA để torch/torchvision khớp bản GPU (đổi cu126 theo CUDA máy bạn).
# Nhờ dòng này, "torch==2.8.0" trong requirements.txt sẽ giải về bản +cu126.
--extra-index-url https://download.pytorch.org/whl/cu126
-r requirements.txt

# MONAI 3D pipeline extras (NIfTI IO, transforms, SwinUNETR, metrics)
scikit-image==0.24.0
einops==0.8.0
tensorboard==2.18.0

# Experiment tracking & config
wandb==0.18.7
pyyaml==6.0.2

# Visualization & misc (dùng trong training/visualize, sanity-check)
matplotlib==3.10.6
scikit-learn==1.5.2
tqdm==4.67.1

# --- Baseline 2D CŨ (TÙY CHỌN, cài riêng) ---
# opencv-python & albumentations đòi numpy>=2 trên Python 3.13, XUNG ĐỘT với MONAI (numpy<2).
# Pipeline MONAI 3D KHÔNG cần chúng. Chỉ khi muốn chạy lại baseline U-Net 2D thì cài
# trong MỘT MÔI TRƯỜNG RIÊNG:
#   pip install opencv-python albumentations
